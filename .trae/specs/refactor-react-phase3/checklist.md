- [x] 在浅色模式下，编辑器的代码高亮（`xq-light.css`）能正常生效，无纯文本退化。
- [x] 输入代码时，CodeMirror 自动匹配并闭合括号等基础体验恢复。
- [x] `UploadImgDialog` 弹窗支持直接拖拽/点击上传单张图片，且包含全部 8 种图床选项。
- [x] 所有的图床参数设置已统一由 Zustand 管理，脱离对原生 `localStorage.setItem` 的直接调用。
- [x] 能解析并迁移 Vue 版本的独立 `doocs-md-` 前缀旧配置（包括主题、自定义图床等），实现无损升级。